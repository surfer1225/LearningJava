package main.java.Company.Grab;

/**
 You receive a bit stream (0 and 1). Continuously receive a stream of bit,
 each time, you have to determine whether the current value is divisibility by 3 and print True or False
 For example:
 */
public class DivisibleIncomingBit {
    /*
    if 1, *2+1, check remainder
    if 0, *2, check remainder
     */
}
